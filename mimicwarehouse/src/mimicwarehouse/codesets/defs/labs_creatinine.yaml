# mimicwarehouse code set (EP-40). ASCII only; the (id, version) pair is frozen in
# codesets.lock.json - bump version to change.
id: labs_creatinine
version: "1.0.0"
name: Serum creatinine (lab itemids)
description: >
  The labevents itemid of serum creatinine as curated by EP-39's item catalogue
  (concept group labs.creatinine, canonical unit mg/dL; mimic-code chemistry.sql keeps
  0 < value <= 150).
kind: itemid
members:
  itemids: [50912]
provenance:
  source: mimic-code
  url: https://github.com/MIT-LCP/mimic-code
  ref: data/item_units.yaml (labs.creatinine) <- mimic-iv/concepts_duckdb/measurement/chemistry.sql
  accessed: "2026-09-06"
references:
  - https://github.com/MIT-LCP/mimic-code
notes: >
  Units and plausibility bounds live in units.py (mwh_harmonize); the chart-side
  "Creatinine (serum)" item is not read by any vendored concept and is not curated, so it
  is left out here.
